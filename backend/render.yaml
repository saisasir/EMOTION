services:
  - type: web
    name: emotion-recognition
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg  # Install ffmpeg system-wide
      pip install -r requirements.txt  # Install Python dependencies
    startCommand: python app.py  # Start Flask app
